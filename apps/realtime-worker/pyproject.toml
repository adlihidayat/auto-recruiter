[project]
name = "realtime-worker"
version = "0.1.0"
description = "Realtime LiveKit Worker for Auto Recruiter"
requires-python = ">=3.12"
dependencies = [
    "livekit-agents>=0.8.0",
    "livekit-plugins-google>=0.5.0",
    "livekit-plugins-silero>=0.6.0",
    "livekit-plugins-deepgram>=0.5.0",
    "httpx>=0.27.0",
    "pydantic>=2.6.0",
    "agents",
    "core-ai-lib",
]

[tool.uv.sources]
agents = { workspace = true }
core-ai-lib = { workspace = true }
